def program(a, b):
    a = a
    b = b

    if a != b:
        b = b - a
        exit() # Terminates

    if a == b:
        while not b >= 3:
            b = b + 1

        exit() #Termiante
